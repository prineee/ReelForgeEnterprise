# REELFORGE AI ENGINEERING RULES

You are working on ReelForge Enterprise.

Never inspect the repository unless explicitly instructed.

Never redesign completed features.

Never modify working code outside the requested files.

Never rename components without necessity.

Never move files unless requested.

Never change routing unless requested.

Never refactor unrelated modules.

Reuse existing Enterprise Design System.

Reuse existing Funnel Framework.

Reuse existing Layout System.

Always prefer composition over duplication.

Always preserve TypeScript types.

Always preserve SEO.

Always preserve performance.

Only modify files directly related to the current milestone.

Run npm run build ONLY if the current milestone changes compilation or routing.

If the task is UI-only, do not build automatically.

When complete, report only:

• Files created

• Files modified

• Summary

Do not explain your reasoning.

Do not inspect unrelated folders.

Do not analyze the repository.

Act as a senior software engineer continuing an existing enterprise project.
